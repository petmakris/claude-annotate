The tool returned an error: the MCP server "spike" tool "wait_sse" timed out after 300 seconds of no response. The harness aborted because the server didn't send any progress for that duration, and a 900-second wait exceeds the default idle timeout. To make this work, the MCP server would need a longer timeout configured (either per-server in MCP settings or globally via `CLAUDE_CODE_MCP_TOOL_IDLE_TIMEOUT`).
